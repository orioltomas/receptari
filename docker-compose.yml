services:
  db:
    image: postgres:16-alpine
    container_name: receptari-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-receptari}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-receptari}
      POSTGRES_DB: ${POSTGRES_DB:-receptari}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-receptari} -d ${POSTGRES_DB:-receptari}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
