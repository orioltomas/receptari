# Postgres (coincideix amb docker-compose.yml)
POSTGRES_USER=receptari
POSTGRES_PASSWORD=receptari
POSTGRES_DB=receptari
POSTGRES_PORT=5432

# URL completa que utilitzarà l'API (apps/api/.env)
DATABASE_URL=postgres://receptari:receptari@localhost:5432/receptari
